// userReducer.js
const initialState = {
    // your initial state properties for the user slice
  };
  
  const userReducer = (state = initialState, action) => {
    // handle actions and update state
    return state;
  };
  
  export default userReducer;
  